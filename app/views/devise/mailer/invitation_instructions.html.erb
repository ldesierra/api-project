<p>Hola <%= @resource.email %>!</p>

<p>Tu solicitud para unirte a No Lo Tires ha sido aceptada. Crea tu usuario con el link de abajo!</p>

<% first_manager = '&amp;first_time=true' if @resource.restaurant.incomplete? %>

<p><%= link_to 'Aceptar Invitacion',
  "#{ENV['APP_HOST']}/restaurant_users/accept_invite?invitation_token=#{@token}#{first_manager}"
  %>
</p>

<% if @resource.invitation_due_at %>
  <p><%= t("devise.mailer.invitation_instructions.accept_until", due_date: l(@resource.invitation_due_at, format: :'devise.mailer.invitation_instructions.accept_until_format')) %></p>
<% end %>

<p>Por favor, ignora este email para no crear la cuenta. </p>
